<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_tickets_list') }}">
            <i class="bi bi-bank2"></i> СмартЛомард
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {% if app.current_route == 'app_tickets_list' %}active{% endif %}"
                       href="{{ path('app_tickets_list') }}">Мои билеты</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.current_route == 'app_client_profile' %}active{% endif %}"
                       href="{{ path('app_client_profile') }}">Профиль</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item d-flex align-items-center">
                    <span class="navbar-text me-3">
                        <i class="bi bi-person-circle"></i> {{ app.user.fullName }}
                    </span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_client_logout') }}">
                        <i class="bi bi-box-arrow-right"></i> Выход
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
